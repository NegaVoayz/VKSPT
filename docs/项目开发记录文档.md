2026-06-08 17:36 确定光谱追踪技术框架，创建项目设计文档
2026-06-08 18:56 引入第三方库：tinyobjloader 1.0.6, tinyxml2 11.0.0, stb
2026-06-08 18:57 创建 CLAUDE.md 项目文档，初始化 Git 仓库，配置 .gitignore（排除构建产物、IDE 文件、OBJ 模型资产）
2026-06-08 19:24 将 CLAUDE.md 加入 .gitignore，取消 Git 追踪
2026-06-08 19:45 Phase 1 基础框架：创建 CMakeLists.txt（C++20, Vulkan 1.4, SDL3, GLM），实现 Window/VulkanContext/AccelerationStructure/RayTracingPipeline/Renderer/Application 类，编写单色光线追踪 Compute Shader（raytrace.comp），成功编译生成 vkrt.exe
2026-06-08 20:00 修复 Vulkan 验证层错误：bufferDeviceAddress 特性启用、队列族匹配、push constant 范围、Surface/Swapchain 销毁顺序、Vulkan 1.4 swapchain 内存绑定、输出图像布局保持 GENERAL、per-image semaphore 防重用、per-frame descriptor sets。程序通过验证层零报警，成功渲染输出 output.ppm
2026-06-08 20:10 输出格式从 PPM 切换为 PNG，集成 stb_image_write，重命名 saveOutputPPM → saveOutputPNG
2026-06-08 20:29 修复三角形着色：光源方向从 (1,2,1) 改为 (1,2,-1)，使其从摄像机侧照亮三角形正面（摄像机在 Z=-5，三角形在 Z=0 面向 +Z）
2026-06-08 21:04 Phase 2 光谱追踪：实现 SpectralRay/Material 数据结构，迁移至 Slang 着色器编译器（slangc），创建 spectral_common.slang 光谱数学库（Cauchy IOR、折射、反射、波长→RGB、光谱吸收），重写 raytrace.slang 支持每像素 16 波长离散光谱采样（sp[401]累积→最终 RGB 积分），构建双三角形棱镜楔形场景（BK7 玻璃材质，Cauchy A/B 色散系数），多弹道折射追踪（入射→玻璃内→出射），新增材质 UBO 描述符绑定，天空改为白色发射体以显示色散效果
2026-06-08 22:13 Phase 2 修复与完成：slangc 生成的 SPIR-V 导致 GPU 挂起（TDR），回退至 glslc 编译器。单文件 raytrace.comp 内联所有光谱数学（Cauchy IOR、Snell 折射、wavelengthToRGB、rgbToDominantWavelength、spectralAbsorption）。实现 16 波长稀疏谱累积 + 白参考归一化（等能输入→白色输出）。三弹道光追循环（入射→玻璃内→出射→天空/表面）。材质 UBO（GpuMaterial std140）通过 binding=2 描述符绑定。AccelerationStructure 重构支持 buildTwoInstance 双 BLAS 双实例 TLAS。修复出射面法线方向（移除多余反转）。验证项：零 Vulkan 验证层错误、天空白色输出、Lambertian 光谱吸收（红色表面优先反射红光、吸收蓝绿）

2026-06-08 23:06 Phase 2.5 漫反射路径追踪：替换硬编码定向光源为 Monte Carlo 路径追踪。新增 GPU RNG（Wang hash，基于像素+采样+弹射的确定性种子）、余弦加权半球采样（Lambertian 表面随机散射方向）。路径追踪循环：每次命中 Lambertian 表面时施加光谱吸收后随机采样新方向继续追踪，天空作为唯一光源（发射体），路径最终逸出时累积 skyColor * energy * wavelengthToRGB。俄罗斯轮盘赌（3 次弹射后以 max(energy,0.1) 概率存活，无偏）。移除 sIsSky 哨兵数组和硬编码 lightDir，统一使用波长加权 RGB 累积。修复 spectralAbsorption：引入饱和度混合（低饱和表面=白色/灰色保持所有波长平坦通过，高饱和表面=纯色保持选择性吸收）。测试场景：红色地板（Y=-1）+ 白色后墙（Z=2）产生可见的色彩渗透（color bleeding）——白墙底部因红色地板间接光照呈现红紫色调（R=126 G=91 B=125 at wall-floor junction vs R=179 G=201 B=211 at wall center）。最大弹射次数从 3 提升至 8。
2026-06-09 00:30 Phase 3 自适应光谱分裂：实现波长范围光线（λ_start–λ_end, nm 整数）代替 Phase 2.5 的单波长采样。新增 per-pixel 局部栈（SPECTRAL_STACK_MAX=16）管理分裂产生的子光线：父光线命中电介质表面后自适应分裂为紫端/红端子光线，能量按带宽比例分配。分裂决策四条件（设计文档§4.2）：预测横向色散分离超过像素阈值、波长范围≥10nm、能量≥0.05、分裂代数≤5。色散向量计算（computeDispersion）：入射光线在 λ_start 和 λ_end 处分别 Snell 折射，差值正交化得到 per-unit-distance 横向展宽。last_split 传播：入射→命中面，last_split += dispersion × tHit；折射时将 last_split/dispersion 旋转到新切平面；反射时镜像；分裂时子光线继承。进入/离开介质追踪（insideGlass 标志）：进入电介质→eta=1/ior，离开电介质→eta=ior，修正 Phase 2 始终 eta=1/ior 的 bug。光谱积分修正：16 参考波长点积分子波长范围，总和归一化（totalWlIntegral）正确处理分裂子光线能量守恒。面法线硬编码：TLAS 实例使用单位矩阵变换→ObjectToWorld 始终为单位矩阵，无法获得几何法线。临时按 instanceCustomIndex 硬编码已知三角形法线（Phase 4 需绑定顶点缓冲为 SSBO 正确计算）。测试场景：单片 BK7 玻璃面 (~37° tilt) 成功触发自适应分裂——用户观察到粉色三角形（调试热力图确认 totalRays > 1 的分裂活跃像素）。两片面棱镜因未闭合几何体导致光线在面间重复弹射丢失，留待 Phase 4+ 完善。：替换硬编码定向光源为 Monte Carlo 路径追踪。新增 GPU RNG（Wang hash，基于像素+采样+弹射的确定性种子）、余弦加权半球采样（Lambertian 表面随机散射方向）。路径追踪循环：每次命中 Lambertian 表面时施加光谱吸收后随机采样新方向继续追踪，天空作为唯一光源（发射体），路径最终逸出时累积 skyColor * energy * wavelengthToRGB。俄罗斯轮盘赌（3 次弹射后以 max(energy,0.1) 概率存活，无偏）。移除 sIsSky 哨兵数组和硬编码 lightDir，统一使用波长加权 RGB 累积。修复 spectralAbsorption：引入饱和度混合（低饱和表面=白色/灰色保持所有波长平坦通过，高饱和表面=纯色保持选择性吸收）。测试场景：红色地板（Y=-1）+ 白色后墙（Z=2）产生可见的色彩渗透（color bleeding）——白墙底部因红色地板间接光照呈现红紫色调（R=126 G=91 B=125 at wall-floor junction vs R=179 G=201 B=211 at wall center）。最大弹射次数从 3 提升至 8。